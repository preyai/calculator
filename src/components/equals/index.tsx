import Button from "../button"
import Wrapper from "../wrapper"

const Equals = () => {
    return (
        <Wrapper columns={1}>
            <Button text="=" iris={true} />
        </Wrapper>
    )
}
export default Equals