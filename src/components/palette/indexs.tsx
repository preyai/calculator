import Draggable from '../draggable';
import styles from './palette.module.scss';

const Palette = () => {
    return (
        <div>
            <Draggable />
            <Draggable />
        </div>
    )
}

export default Palette